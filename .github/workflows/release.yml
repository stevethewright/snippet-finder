name: Build & Release

on:
  push:
    branch:
      - '**'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version:
          - "3.12"
    runs-on: ${{ matrix.os }}

    # Clean previous builds - Run for Ubuntu and MacOS
    steps:
    - name: Clean previous builds (Linux/Mac)
      if: ${{ runner.os != 'Windows' }}
      run: |
        rm -rf dist/ build/ __pycache__

    # Clean previous builds - Run only on Windows OS
    - name: Clean previous builds (Windows)
      if: ${{ runner.os == 'Windows' }}
      run: |
        Remove-Item -Recurse -Force dist
        Remove-Item -Recurse -Force build
        Remove-Item -Recurse -Force __pycache__

    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir -r requirements.txt

    - name: Install PyInstaller
      run: |
        pip install pyinstaller

    - name: Build with PyInstaller
      run: |
        PYTHONPATH=src pyinstaller --onefile -n snippet_finder --distpath dist/ src/snippet_finder/__main__.py

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: build-${{ matrix.os }}
        path: dist/
  
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-ubuntu-latest
    - name: Create GitHub Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref }}
        name: Release ${{ github.ref }}
        draft: false
        prerelease: false
    - name: Upload release assets
      uses: softprops/action-gh-release@v1
      with:
        assets: |
          dist/*   # or path(s) to OS-specific executables